import React from "react";

const BannerSeccion = (props) => {
    return(
        <div class='justify-center text-center'>
            <h1 class=''>{props.title}</h1>
        </div>
    )
};

export default BannerSeccion;